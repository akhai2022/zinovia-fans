[project]
name = "zinovia-worker"
version = "0.1.0"
description = "Zinovia Fans worker"
requires-python = ">=3.12"
dependencies = [
  "celery>=5.4.0",
  "redis>=5.0.0",
  "pillow>=10.4.0",
  "boto3>=1.34.0",
]

[project.optional-dependencies]
dev = [
  "ruff>=0.5.0",
  "mypy>=1.10.0",
]
